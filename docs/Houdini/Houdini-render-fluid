# 如何使用Houdini渲染流体？

渲染器与材质是直接关联的，不同渲染器有不同的灯光、材质。

我们首先讲解如何利用Houdini自带的Mantra渲染（CPU渲染，较慢），然后讲解更快的第三方渲染器Redshift（GPU渲染，较快）。

但是，在此之前，有一些通用步骤，即读取流体粒子帧数据和表面重建。

## 读取数据和流体表面重建

首先，我们准备粒子数据。我这里的输入数据是从taichi生成的mpm3d模拟得到的序列

如果是obj格式的话，
它里面只有一些点而已
